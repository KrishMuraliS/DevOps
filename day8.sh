#!/bin/bash

#Below loop executes the command between do and done based on the result
while <condition>
do
    <command 1>
    <command 2>
done

#Below loop executes the command between do and done for items in the list
for <var> in <value1 value2 ... valuen>
do
    <command 1>
    <command 2>
    <etc>
done

#Below loop executes the command between do and done until the condition is true
until <condition>
do
    <command 1>
    <command 2>
    <etc>
done
